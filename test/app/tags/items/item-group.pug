item-group
  a.block.bg-white.p16(href='/{opts.item.ref.path}')
    div {opts.item.data.title}

  style(type='less').
    :scope {
      display: block;
    }

  script.
    this.on('mount', () => {
      //- document.addEventListener('click', () => {
      //-   console.log('foo');
      //- });
    });
